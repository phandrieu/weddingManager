<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1"> {# 🔑 important pour la responsivité #}
        <title>{% block title %}Notre Messe de Mariage{% endblock %}</title>
        
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

        <style>
            /* === Intégration des polices locales === */
            @font-face {
                font-family: 'Alfarn';
                src: url('{{ asset("fonts/Alfarn-Regular.ttf") }}') format("truetype");
                font-weight: 400;
                font-style: normal;
            }

            @font-face {
                font-family: 'Avenir LT Pro';
                src: url('{{ asset("fonts/AvenirLTPro.ttf") }}') format("truetype");
                font-weight: 400;
                font-style: normal;
            }

            /* Palette de couleurs */
            :root {
                --color-bg: #FFFFFF;
                --color-primary: #FF8100;
                --color-secondary: #6B48A1;
                --color-tertiary: #E6DEF6;
                --color-last: #61616F;
                /* padding utilisé par le container pour positionner les titres */
                --container-padding: 1rem;
            }
            
            /* Typographie */
            body {
                font-family: 'Kanit', Avenir, Helvetica, Arial, sans-serif;
                background-color: var(--color-bg);
                min-height: 100vh;
                display: flex;
                flex-direction: column;
                color: var(--color-last);
            }

            h1, h2, h3 {
                font-family: 'Alfarn', serif;
                font-weight: 400;
                text-transform: uppercase;
                color: var(--color-primary);
            }
            
            /* Spécifique aux H1 demandées */
            h1 {
                font-size: 20px;                         /* taille demandée */
                font-weight: 600;
                text-transform: uppercase;
                letter-spacing: .03em;
                color: var(--color-primary);             /* texte orange */
                background-color: var(--color-secondary);/* rectangle violet (rempli) */
                display: inline-block;                   /* boîte compacte */
                width: 500px;                            /* largeur comme sur la maquette */
                text-align: center;                      /* texte centré dans la boîte */
                padding: 0.45rem 0;                      /* hauteur de la boîte */
                margin: 0 0 1rem calc(-1 * var(--container-padding)); /* bord gauche à l'écran */
                box-sizing: border-box;
                border-radius: 0;
                margin-top: 0;

                transform: translate(-2rem, -2.20rem);
            }

            h4, h5, h6 {
                font-family: 'Alfarn', serif;
                font-weight: 400;
                color: var(--color-secondary);
            }

            p, li, span, label, input, select, textarea {
                font-family: 'Avenir LT Pro', Avenir, Helvetica, Arial, sans-serif;
                font-size: 1rem;
            }

            /* Navbar */
            .navbar {
                background-color: #6B48A1;
                padding-top: 6px;
                padding-bottom: 0;
                /*border-bottom: 2px solid var(--color-primary);*/
            }

            .navbar-brand {
                font-weight: 600;
                font-size: 1.2rem;
                color: var(--color-primary) !important;
                font-family: 'Alfarn', serif;
                text-transform: uppercase;
            }

            .navbar-nav .nav-link {
                margin-right: .75rem;
                color: var(--color-bg) !important;
                transition: color .2s;
            }
            .navbar-nav .nav-link:hover {
                color: var(--color-primary) !important;
            }

            /* Boutons */
            .btn-primary {
                background-color: var(--color-primary);
                border-color: var(--color-primary);
            }
            .btn-primary:hover {
                background-color: #e56f05;
                border-color: #e56f05;
            }

            .btn-secondary {
                background-color: var(--color-secondary);
                border-color: var(--color-secondary);
            }
            .btn-secondary:hover {
                background-color: #5823c2;
                border-color: #5823c2;
            }

            .btn-success {
                background-color: var(--color-tertiary);
                border-color: var(--color-tertiary);
                color: #fff;
            }
            .btn-success:hover {
                background-color: #81956c;
                border-color: #81956c;
            }

            /* Conteneur */
            .container {
                max-width: 100%;
                padding-left: var(--container-padding);
                padding-right: var(--container-padding);
            }

            /* Responsive */
            @media (max-width: 576px) {
                /* ajustement du padding pour petits écrans afin que la marge négative suive */
                :root { --container-padding: .75rem; }
                 .container {
                    padding-left: var(--container-padding);
                    padding-right: var(--container-padding);
                 }
                 h1 {
                     width: 160px; /* réduit sur petit écran */
                     font-size: 20px;
                     margin-left: calc(-1 * var(--container-padding));
                 }
                 h2, h3 { font-size: 1.25rem; }
             }

            /* Footer */
            footer {
                margin-top: auto;
                padding: 1rem;
                text-align: center;
                background: #fff;
                border-top: 1px solid #ddd;
                font-size: 0.9rem;
            }
        </style>

        {% block stylesheets %}{% endblock %}
        {{ encore_entry_link_tags('app') }}
    </head>
    <body>
        {% if app.user %}
        <nav class="navbar navbar-expand-lg shadow-sm sticky-top">
            <div class="container">
                <a class="navbar-brand d-flex align-items-center" style="font-size:24px;" href="{{ path('public') }}">
                    <img src="{{ asset('logos/logo_white_nobg_noborder.png') }}" alt="Logo" style="height:45px; width:59px; object-fit:cover; transform: translate(0px, -7px);" class="me-2">
                    Notre Messe de Mariage
                </a>  
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" 
                        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0" style="margin-left: 30px;">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('home') }}">{% if 'ROLE_ADMIN' in app.user.roles or 'ROLE_MUSICIAN' in app.user.roles %}Accueil{% else %}Mon mariage{% endif %}</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_song_index') }}">Répertoire global</a>
                        </li>
                        {% if 'ROLE_MUSICIAN' in app.user.roles %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_repertoire_index') }}">Répertoire personnel</a>
                        </li>
                        {% endif %}
                        {% if 'ROLE_ADMIN' in app.user.roles or 'ROLE_MUSICIAN' in app.user.roles %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_wedding_index') }}">Mariages</a>
                        </li>
                        {% endif %}
                        {% if 'ROLE_ADMIN' in app.user.roles %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_songtype_index') }}">Types de chants</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_user_index') }}">Utilisateurs</a>
                            </li>
                        {% endif %}
                    </ul>
                    <a href="{{ path('app_logout') }}" class="nav-link" title="Se déconnecter">
                        <span class="me-1" style="color: var(--color-bg);">{{ app.user.firstName }}</span>
                        <i style="color: var(--color-bg);" class="bi bi-box-arrow-right"></i>
                    </a>
                </div>
            </div>
        </nav>
        {% endif %}

        <div class="container mt-4">
            {% block body %}{% endblock %}
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}{% endblock %}
        {{ encore_entry_script_tags('app') }}
    </body>
</html>