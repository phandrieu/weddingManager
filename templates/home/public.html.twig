{% extends 'base.html.twig' %}

{% block title %}Notre Messe de Mariage{% endblock %}

{% block body %}
<style>
    /* Fonts et couleurs */
    body {
        font-family: "Avenir LT Pro", sans-serif;
        background-color: #FFFCF2;
    }
    h1, h2, h3 {
        font-family: "Alfarn", serif;
        text-transform: uppercase;
        color: #652D90;
    }
    h1 span {
        color: #FA8211;
    }
    .btn-primary {
        background-color: #FA8211;
        border: none;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
    .btn-primary:hover {
        transform: translateY(-3px);
        box-shadow: 0 5px 15px rgba(250, 130, 17, 0.4);
    }
    .section {
        padding: 5rem 2rem;
    }
    .section-alt {
        background-color: #f8f9fb;
    }
    .fade-in {
        opacity: 0;
        transform: translateY(30px);
        transition: all 0.8s ease;
    }
    .fade-in.visible {
        opacity: 1;
        transform: translateY(0);
    }
</style>

<!-- Hero -->
<section class="position-relative text-center text-white" style="height: 100vh; background: url('{{ asset('images/wide1.jpg') }}') center/cover no-repeat;">
    <div class="d-flex flex-column justify-content-center align-items-center h-100 bg-dark bg-opacity-50">
        <h1 class="display-3 fw-bold">NOTRE MESSE DE MARIAGE</h1>
        <p class="fs-2 fw-bold">La solution pour orchestrer votre cérémonie</p>
        <a href="{{ path('app_register') }}" class="btn btn-primary btn-lg mt-3">Inscription / Connexion</a>
    </div>
</section>

<!-- À propos -->
<section class="section fade-in text-center">
    <h2 class="mb-4 text-primary">À propos</h2>
    <p class="fs-5">
        <em>Notre Messe de Mariage</em> est un outil en ligne conçu pour accompagner <strong>tous les acteurs de la célébration</strong> — couples, paroisses et musiciens — dans la préparation pas à pas d’une messe ou d’une cérémonie de mariage.
    </p>
    <p>
        Vous gagnez du <strong>temps</strong>, de la <strong>clarté</strong> et la certitude de vivre une célébration <strong>belle, fidèle et porteuse d’émotion</strong>.
    </p>
</section>

<!-- Futurs mariés -->
<section class="section section-alt fade-in">
    <div class="container">
        <div class="row align-items-center g-4">
            <div class="col-lg-6">
                <h2 class="mb-3">💍 Fonctionnalités pour les futurs mariés</h2>
                <p>
                    Textes, chants, prières, coordination avec vos musiciens et votre paroisse… <br>
                    Notre Messe de Mariage devient votre compagnon de route pour avancer pas à pas.
                </p>
                <ul>
                    <li>📖 Choisir vos textes (lectures, prières, bénédictions…)</li>
                    <li>🎶 Construire votre répertoire musical</li>
                    <li>🗂️ Générer automatiquement vos livrets et partitions</li>
                </ul>
                <p class="mt-3 text-primary">Prix : <strong>49,99€</strong> (paiement unique)</p>
                <a href="{{ path('app_register') }}" class="btn btn-primary">Créez votre cérémonie</a>
            </div>
            <div class="col-lg-6 text-center">
                <img src="{{ asset('images/vertical1.jpg') }}" alt="Futurs mariés" class="img-fluid rounded shadow">
            </div>
        </div>
    </div>
</section>

<!-- Paroisses -->
<section class="section fade-in">
    <div class="container">
        <div class="row align-items-center g-4 flex-lg-row-reverse">
            <div class="col-lg-6">
                <h2 class="mb-3">⛪ Fonctionnalités pour les paroisses</h2>
                <p>
                    Suivez les couples, validez leurs choix et gardez la maîtrise liturgique de chaque célébration.
                </p>
                <ul>
                    <li>🗂️ Tableau de bord centralisé</li>
                    <li>📖 Validation des textes et prières</li>
                    <li>🎶 Suivi du répertoire musical</li>
                </ul>
                <p class="mt-3 text-success">Prix : <strong>Offert</strong></p>
                <a href="{{ path('app_register') }}" class="btn btn-primary">Accéder à l’espace paroisse</a>
            </div>
            <div class="col-lg-6 text-center">
                <img src="{{ asset('images/vertical2.jpg') }}" alt="Paroisse" class="img-fluid rounded shadow">
            </div>
        </div>
    </div>
</section>

<!-- Musiciens -->
<section class="section section-alt fade-in">
    <div class="container">
        <div class="row align-items-center g-4">
            <div class="col-lg-6">
                <h2 class="mb-3">🎶 Fonctionnalités pour les musiciens</h2>
                <p>
                    Disposez d’un espace unique pour piloter toute votre activité liée aux mariages.
                </p>
                <ul>
                    <li>📂 Répertoire personnel enrichi</li>
                    <li>📑 Génération automatique de partitions</li>
                    <li>🤝 Collaboration simplifiée avec les couples</li>
                </ul>
                <a href="{{ path('app_register') }}" class="btn btn-primary">Accéder à l’espace musicien</a>
            </div>
            <div class="col-lg-6 text-center">
                <img src="{{ asset('images/vertical3.jpg') }}" alt="Musicien" class="img-fluid rounded shadow">
            </div>
        </div>
    </div>
</section>

<!-- Footer -->
<footer class="text-center py-4">
    <img src="{{ asset('logos/horizontal_onde_blanche.png') }}" alt="Logo" class="mb-2" style="height:80px;">
    <p class="mb-0">© {{ "now"|date("Y") }} Notre Messe de Mariage. Tous droits réservés.</p>
</footer>

<script>
    // Animation au scroll
    document.addEventListener("DOMContentLoaded", function() {
        const elements = document.querySelectorAll(".fade-in");
        const observer = new IntersectionObserver(entries => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add("visible");
                    observer.unobserve(entry.target);
                }
            });
        }, { threshold: 0.2 });

        elements.forEach(el => observer.observe(el));
    });
</script>
{% endblock %}